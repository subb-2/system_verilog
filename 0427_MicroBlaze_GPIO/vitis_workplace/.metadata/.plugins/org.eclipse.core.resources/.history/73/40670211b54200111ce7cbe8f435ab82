/*
 * ap_main.c
 *
 *  Created on: 2026. 4. 28.
 *      Author: kccistc
 */

#include "ap_main.h"
#include "../common/common.h"
#include "UPCounter/UPCounter.h"


void ap_init() {
	UPCounter_Init();
}

void ap_excute() {
	uint16_t counter = 0;
	//FND_SetNum(1234);
	//uint32_t prevTimeDisp = 0;
	uint32_t prevTimeCounter = 0;

	while(1)
	{
		UPCounter_Excute();
		delay_ms(1);
		millis_inc();
	}
}
